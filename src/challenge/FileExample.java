package challenge;

import java.io.*;

public class FileExample {
    public static void main(String[] args) throws IOException {
        /*File result1=new File("Test.txt");
        FileWriter fileWriter=new FileWriter(new BufferedWriter(result1));
        fileWriter.write("Maths-Logical",0,5);
        fileWriter.flush();
        fileWriter.close();
        System.out.println(new BufferedReader(new FileReader(result1)).readLine());*/
    }
}
